#!/usr/bin/perl

use strict;
use warnings;
use 5.010;

use Pod::Usage;

use constant {
	VERSION_NAME   => 'git-merge-sync',
	VERSION_NUMBER => '0.0.0',
};
use constant {
	VERSION => VERSION_NAME . '/' . VERSION_NUMBER,
};

# подробный man программы
sub usage_man() {
	use Pod::Man;

	my $parser = Pod::Man->new(
		center  => VERSION_NAME . " Manual",
		release => VERSION_NUMBER,
	);
	if ( open(my $man, "|man -l -") ) {
		$parser->parse_from_file(__FILE__, $man);
		close $man;
		exit 0;
	}
	else {
		warn "Can't create a pipe to man tool\n";
		warn "Try to execute:  pod2man '$0' | man -l -\n";
		exit 1;
	}
}

my (%opt, @branches);

sub process_info_options() {
	pod2usage(1)
		if defined $opt{help}
	;

	usage_man()
		if defined $opt{man}
	;

	say(VERSION) and exit(0)
		if defined $opt{version}
	;
}

sub load_options() {
	use Getopt::Long;

	Getopt::Long::Configure("bundling");
	GetOptions(\%opt,
		"help|h|?",
		"man",
		"version",
	)
		or pod2usage(2)
	;

	process_info_options();

	@branches = @ARGV
		or pod2usage(2)
	;
}

sub check_if_repository() {
	`git status --short`;
	$? && die "E! Not in Git repository. Aborting\n";
}

sub check_if_branches() {
	my $repo_branches = `git branch`;
	$? && die "E! Unable to get branches list of the repository. Aborted\n";

	my %unknown_branches = map { $_ => 1 } @branches;

	my @all_branches = split(/\n/, $repo_branches);
	for my $branch (@all_branches) {
		$branch =~ s/[\s*]+//;
		delete $unknown_branches{$branch};
	}

	if (%unknown_branches) {
		warn "E! There are no such branches in the repository:\n";
		warn "E!     * $_\n" for keys %unknown_branches;
		die  "E! Aborted\n";
	}
}

sub validate_asked() {
	check_if_repository();

	check_if_branches();
}


load_options();
validate_asked();

__END__;

=pod

=head1 NAME

git-merge-sync - a tool for merging and syncronizing cascaded branches
in Git repository

=head1 SYNOPSIS

B<git-merge-sync> [I<options>] [B<-->] I<branch> ...

B<git-merge-sync> (B<-h> | B<-?> | B<--help> | B<--man> | B<--version>)

=head1 DESCRIPTION

=head1 OPTIONS

=over

=item B<-h>, B<-?>, B<--help>

Print short help about usage and exit.

=item B<--man>

Display full man and exit.

=item B<--version>

Print program version and exit.

=item I<branch>

Branches names for work on. At least one branch is required.

=back

=head1 AUTHORS

Copyright (C) 2013 Vovan-VE <vovan-ve@yandex.ru>

=cut
